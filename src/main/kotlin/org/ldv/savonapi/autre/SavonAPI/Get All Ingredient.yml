info:
  name: Get All Ingredient
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:8080/api-savon/v1/ingredient
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
